<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="main min-h-screen bg-zinc-900">
      <h1 class="text-center text-6xl text-white mb-10">Create Your Task</h1>
      <div class="form p-10 w-full">
        <form
          action="/create"
          method="post"
          class="flex flex-col w-1/2 mx-auto gap-5"
        >
          <input
            class="bg-zinc-800 p-2 text-lg text-white outline-none border-[1px] border-zinc-500 rounded-md"
            type="text"
            name="title"
            id="title"
            placeholder="Enter Your title"
          />
          <textarea
            class="bg-zinc-800 p-2 text-lg text-white outline-none border-[1px] border-zinc-500 resize-none rounded-md"
            name="description"
            id="description"
            placeholder="Enter Your Description"
          ></textarea>
          <input
            class="bg-blue-700 py-2 px-5 text-lg text-white outline-none border-[1px] border-blue-500 rounded-md hover:bg-blue-800 cursor-pointer"
            type="submit"
            value="Create a Task"
          />
        </form>
      </div>

      <div
        class="cards p-10 w-full max-w-full mx-auto space-y-6 flex flex-wrap gap-6 mt-7"
      >
        <% if (files.length > 0) { %> <% files.forEach((data) => { %>
        <div
          class="bg-zinc-700 rounded-xl p-5 shadow-md hover:shadow-lg transition-shadow duration-300 ease-in-out w-full sm:w-1/2 md:w-1/3 lg:w-1/4 flex flex-col h-full"
        >
          <h2 class="text-white text-2xl font-semibold mb-3"><%= data %></h2>
          <div class="flex justify-between items-center">
            <a
              href="/file/<%= data %>"
              class="text-blue-500 hover:text-blue-400 text-lg font-medium group"
              >Read More.
              <span
                class="hidden group-hover:inline transition-opacity duration-300 opacity-0 group-hover:opacity-100"
                >📖</span
              ></a
            >
            <a
              href="/edit/<%= data %>"
              class="text-blue-500 hover:text-blue-400 text-lg font-medium group"
            >
              Edit Filename
              <span
                class="hidden group-hover:inline transition-opacity duration-300 opacity-0 group-hover:opacity-100"
                >✏️</span
              >
            </a>
          </div>
        </div>
        <% }) %> <% } else { %>
        <p class="text-white text-2xl font-medium">No data</p>
        <% } %>
      </div>
    </div>
  </body>
</html>
