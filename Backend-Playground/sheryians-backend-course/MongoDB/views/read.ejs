<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Users</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="bg-zinc-800 min-h-screen text-white">
      <h1 class="text-center p-10 text-blue-600 text-5xl font-mono">
        All Users 📋
      </h1>

      <nav class="ml-20 mb-20">
        <a href="/" class="text-blue-500 hover:text-blue-200 text-lg">Home</a>
      </nav>

      <div
        class="card-wraper p-10 flex flex-wrap gap-6 items-center justify-start"
      >
        <% if (users && users.length > 0) { %> <% users.forEach((user) => { %>
        <div
          class="card bg-zinc-700 text-white rounded-2xl shadow-lg overflow-hidden p-4"
        >
          <div class="img">
            <img
              src="<%= user.imageUrl %>"
              alt="user image"
              class="w-full h-48 object-cover rounded-lg"
            />
          </div>
          <div class="user-info mt-4">
            <h2 class="text-xl font-semibold"><%= user.name %></h2>
            <p class="text-sm text-gray-300 mt-2"><%= user.email %></p>
          </div>
          <div class="links mt-4 flex justify-between">
            <a href="/edit/<%= user._id %>" class="text-blue-400 hover:text-blue-200">Edit</a>
            <a href="/delete/<%= user._id %>" class="text-red-400 hover:text-red-200">Delete User</a>
          </div>
        </div>
        <% }) %> <% } else { %>
        <p class="text-white text-2xl font-medium">No Users</p>
        <% } %>
      </div>
    </div>
  </body>
</html>
