<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="bg-zinc-800 h-screen w-full">
      <h1 class="text-center p-10 text-blue-600 text-5xl font-mono">
        Register 📝
      </h1>
      <nav class="ml-25 mb-15">
        <a href="/login" class="text-blue-500 hover:text-blue-200">Login 🔐</a>
      </nav>

      <div class="form">
        <form
          action="/register"
          method="post"
          class="flex flex-col items-center justify-center p-10 gap-6 text-white"
        >
          <!-- UserName Input -->
          <input
            type="text"
            name="userName"
            id="userName"
            placeholder="UserName"
            class="border border-zinc-700 bg-transparent px-6 py-4 text-xl font-mono outline-none rounded-lg w-96 min-w-[300px] focus:ring-2 focus:ring-blue-600 focus:border-transparent"
          />
          <!-- Name Input -->
          <input
            type="text"
            name="name"
            id="name"
            placeholder="Name"
            class="border border-zinc-700 bg-transparent px-6 py-4 text-xl font-mono outline-none rounded-lg w-96 min-w-[300px] focus:ring-2 focus:ring-blue-600 focus:border-transparent"
          />

          <!-- Age Input -->
          <input
            type="number"
            name="age"
            id="age"
            placeholder="Age"
            class="border border-zinc-700 bg-transparent px-6 py-4 text-xl font-mono outline-none rounded-lg w-96 min-w-[300px] focus:ring-2 focus:ring-blue-600 focus:border-transparent"
          />

          <!-- Email Input -->
          <input
            type="email"
            name="email"
            id="email"
            placeholder="Email"
            class="border border-zinc-700 bg-transparent px-6 py-4 text-xl font-mono outline-none rounded-lg w-96 min-w-[300px] focus:ring-2 focus:ring-blue-600 focus:border-transparent"
          />

          <!-- Password Input -->
          <input
            type="password"
            name="password"
            id="password"
            placeholder="Password"
            class="border border-zinc-700 bg-transparent px-6 py-4 text-xl font-mono outline-none rounded-lg w-96 min-w-[300px] focus:ring-2 focus:ring-blue-600 focus:border-transparent"
          />

          <!-- Submit Button -->
          <input
            type="submit"
            value="Register"
            class="bg-blue-700 hover:bg-blue-900 cursor-pointer w-96 min-w-[300px] rounded-lg font-mono text-lg py-2 focus:ring-2 focus:ring-blue-600"
          />
        </form>
      </div>
    </div>
  </body>
</html>
